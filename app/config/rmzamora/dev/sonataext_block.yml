rmzamora_sonata_ext_block:
    # -------------------------------------------------------
    # Override sonata block bundle cache settings
    # -------------------------------------------------------
    #
    # when RmzamoraSonataExtBlockBundle is installed
    # all cache settings set on block bundle will be overriden
    #
    #
    cache:
        enabled:        false # enable block caching
        default_ttl:    0   # default ttl is not set per block
        shared_ttl:     0   # default ttl is not set per block
#        blocks:
#            sonata.block.service.text:        {cache: sonata.cache.mongo, default_ttl: 60, shared_ttl: 60}
#            sonata.page.block.container:      {cache: sonata.cache.mongo, default_ttl: 60, shared_ttl: 60}
#            sonata.page.block.children_pages: {cache: sonata.cache.mongo, default_ttl: 60, shared_ttl: 60}
#            sonata.block.service.rss:         {cache: sonata.cache.mongo, default_ttl: 60, shared_ttl: 60}
#            sonata.media.block.media:         {cache: sonata.cache.mongo, default_ttl: 60, shared_ttl: 60}
#            sonata.media.block.gallery:       {cache: sonata.cache.mongo, default_ttl: 60, shared_ttl: 60}
#            sonata.media.block.feature_media: {cache: sonata.cache.mongo, default_ttl: 60, shared_ttl: 60}
